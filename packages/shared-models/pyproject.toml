[project]
name = "shared-models"
version = "0.1.0"
description = "SnapStudy — Shared Pydantic response schemas for API contracts"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.10.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
